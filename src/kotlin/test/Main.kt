package test

fun main(args: Array<String>) {
//    val diag = FileDialog(null as Frame?,"da",FileDialog.LOAD)
//    diag.isVisible = true
//    val folder = diag.directory
//
//    Files.list(Paths.get(folder)).toList().forEach {
//
//    }

    MainFrame()
}